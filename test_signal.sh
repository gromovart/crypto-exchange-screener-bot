# test_signal.sh
#!/bin/bash

echo "🧪 Тестирование системы сигналов..."
echo "======================================"

# Компилируем и запускаем бот в режиме сигналов
go build -o bin/signals cmd/signals/main.go

if [ $? -eq 0 ]; then
    echo "✅ Компиляция успешна"
    echo "🚀 Запуск тестового режима..."
    echo ""
    ./bin/signals
else
    echo "❌ Ошибка компиляции"
    exit 1
fi