Отлично! Теперь я вижу реальную структуру проекта. Вот полный и исправленный README.md с правильными путями:

# Crypto Exchange Screener Bot - Система управления и развертывания

Полный набор скриптов для управления и развертывания Crypto Exchange Screener Bot на сервере Ubuntu.

## 📁 Структура проекта

```
crypto-exchange-screener-bot/
├── deploy/                     # Директория развертывания
│   ├── scripts/               # Скрипты развертывания и управления
│   │   ├── deploy.sh          # Основной скрипт развертывания
│   │   ├── service.sh         # Скрипт управления службой
│   │   ├── update.sh          # Скрипт обновления
│   │   ├── check-connection.sh # Проверка подключения
│   │   └── README.md          # Документация скриптов
│   ├── docker/                # Docker конфигурации
│   │   ├── docker-compose.db.yml
│   │   ├── dockerfile
│   │   └── persistence
│   │       └── postgres
│   │           └── migrations
│   └── systemd/               # Systemd конфигурации
│       └── crypto-screener-bot.service
├── configs/                   # Конфигурации приложения
│   ├── dev/
│   ├── prod/
│   └── test/
└── README.md                  # Эта документация
```

## 🚀 Быстрый старт

### 1. Предварительные требования

- **Локальная машина**: Linux/macOS с установленным SSH
- **Сервер**: Ubuntu 22.04 (или выше) с публичным IP
- **Доступ**: Root доступ к серверу
- **SSH**: Сгенерированный SSH ключ

### 2. Подготовка репозитория

```bash
# Перейдите в директорию проекта
cd crypto-exchange-screener-bot

# Сделайте скрипты исполняемыми
chmod +x deploy/scripts/*.sh

# Проверьте структуру
ls -la deploy/scripts/
```

### 3. Настройка конфигурации

```bash
# Проверьте продакшен конфиг
ls -la configs/prod/.env

# Если нет конфига, создайте минимальный
cp configs/prod/.env.example configs/prod/.env
```

### 4. Первичное развертывание

```bash
# Запустите развертывание (из корня проекта)
./deploy/scripts/deploy.sh --ip=95.142.40.244 --user=root

# Или с указанием SSH ключа
./deploy/scripts/deploy.sh --ip=95.142.40.244 --user=root --key=~/.ssh/id_rsa
```

## 🛠️ Скрипт развертывания (deploy.sh)

### Основные функции

1. **Проверка подключения**: SSH доступ и авторизация ключа
2. **Установка зависимостей**: Go 1.21+, PostgreSQL 15, Redis
3. **Настройка безопасности**: Firewall (UFW), пользователи, права
4. **Копирование кода**: Сборка и установка приложения
5. **Конфигурация**: Настройка переменных окружения
6. **Systemd сервис**: Создание службы для автоматического запуска
7. **Миграции БД**: Автоматическое применение миграций
8. **Запуск и проверка**: Старт приложения и верификация

### Использование

```bash
# Из корня проекта
./deploy/scripts/deploy.sh [OPTIONS]

# Примеры:
./deploy/scripts/deploy.sh --help
./deploy/scripts/deploy.sh --ip=95.142.40.244 --user=root
./deploy/scripts/deploy.sh --ip=192.168.1.100 --user=ubuntu
```

### Параметры

| Параметр | По умолчанию | Описание |
|----------|--------------|----------|
| `--ip` | `95.142.40.244` | IP адрес сервера |
| `--user` | `root` | Имя пользователя для подключения |
| `--key` | `~/.ssh/id_rsa` | Путь к SSH ключу |
| `--help` | - | Показать справку |

## ⚙️ Скрипт управления службой (service.sh)

### Команды управления

```bash
# Основные команды
./deploy/scripts/service.sh start          # Запустить службу
./deploy/scripts/service.sh stop           # Остановить службу
./deploy/scripts/service.sh restart        # Перезапустить службу
./deploy/scripts/service.sh status         # Показать статус службы
./deploy/scripts/service.sh logs [N]       # Показать N строк логов
./deploy/scripts/service.sh logs-follow    # Просмотр логов в реальном времени
./deploy/scripts/service.sh logs-error     # Показать только ошибки

# Мониторинг и диагностика
./deploy/scripts/service.sh monitor        # Мониторинг состояния системы
./deploy/scripts/service.sh health         # Проверка здоровья системы
./deploy/scripts/service.sh config-show    # Показать текущую конфигурацию
./deploy/scripts/service.sh config-check   # Проверить конфигурацию

# Утилиты
./deploy/scripts/service.sh backup         # Создать резервную копию
./deploy/scripts/service.sh cleanup        # Очистка старых логов
```

### Примеры использования

```bash
# Проверка статуса
./deploy/scripts/service.sh status --ip=95.142.40.244

# Просмотр логов
./deploy/scripts/service.sh logs 100
./deploy/scripts/service.sh logs-follow

# Диагностика
./deploy/scripts/service.sh health
./deploy/scripts/service.sh monitor

# Управление данными
./deploy/scripts/service.sh backup
./deploy/scripts/service.sh config-check
```

## 🔄 Скрипт обновления (update.sh)

```bash
# Обновление приложения на сервере
./deploy/scripts/update.sh --ip=95.142.40.244 --user=root

# Без остановки сервиса (горячее обновление)
./deploy/scripts/update.sh --hot --ip=95.142.40.244
```

## 🔍 Скрипт проверки подключения (check-connection.sh)

```bash
# Проверка подключения к серверу
./deploy/scripts/check-connection.sh --ip=95.142.40.244 --user=root

# Полная диагностика
./deploy/scripts/check-connection.sh --full --ip=95.142.40.244
```

## 🔧 Конфигурация после установки

После развертывания необходимо настроить конфигурационный файл на сервере:

```bash
# Подключитесь к серверу
ssh root@95.142.40.244

# Отредактируйте конфиг
nano /opt/crypto-screener-bot/.env
```

### Обязательные настройки

```env
# ========== Telegram настройки ==========
TELEGRAM_ENABLED=true
TELEGRAM_BOT_TOKEN=ваш_токен_бота_от_BotFather
TELEGRAM_ADMIN_IDS=ваш_telegram_id

# ========== API ключи биржи ==========
# Выберите одну биржу:

# Для Binance:
BINANCE_API_KEY=ваш_api_ключ
BINANCE_API_SECRET=ваш_api_секрет
EXCHANGE=binance

# Или для Bybit:
BYBIT_API_KEY=ваш_api_ключ
BYBIT_API_SECRET=ваш_api_секрет
EXCHANGE=bybit

# ========== Безопасность ==========
JWT_SECRET=$(openssl rand -base64 32)
ENCRYPTION_KEY=$(openssl rand -base64 32)

# ========== База данных ==========
# Эти настройки уже настроены, проверьте:
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crypto_screener_db
DB_USER=crypto_screener
DB_PASSWORD=SecurePass123!  # Смените на свой!
DB_ENABLE_AUTO_MIGRATE=true
```

После изменения конфигурации перезапустите сервис:

```bash
./deploy/scripts/service.sh restart --ip=95.142.40.244
```

## 📊 Мониторинг

### Команды мониторинга

```bash
# Комплексный мониторинг системы
./deploy/scripts/service.sh monitor --ip=95.142.40.244

# Проверка здоровья
./deploy/scripts/service.sh health --ip=95.142.40.244

# Быстрый статус
./deploy/scripts/service.sh status --ip=95.142.40.244
```

### Прямые команды на сервере

```bash
# Подключитесь к серверу
ssh root@95.142.40.244

# Логи приложения
journalctl -u crypto-screener.service -f
tail -f /var/log/crypto-screener-bot/app.log

# Системные метрики
htop  # Процессы
df -h # Дисковое пространство
free -h # Память

# База данных
sudo -u postgres psql -d crypto_screener_db -c "SELECT COUNT(*) FROM alerts;"

# Redis
redis-cli info
```

## 🗄️ Резервное копирование

### Создание резервной копии

```bash
# Создание полной резервной копии
./deploy/scripts/service.sh backup --ip=95.142.40.244
```

Резервные копии содержат:
- Конфигурацию приложения
- Исходный код
- Дамп базы данных PostgreSQL
- Бинарные файлы

Файлы сохраняются в `/opt/crypto-screener-bot_backups/`

### Восстановление из резервной копии

```bash
# На сервере
cd /opt/crypto-screener-bot_backups/
tar -xzf manual_backup_20240101_120000.tar.gz
```

### Автоматическая очистка

```bash
# Очистка старых файлов
./deploy/scripts/service.sh cleanup --ip=95.142.40.244
```

Удаляет:
- Логи старше 30 дней
- Резервные копии кроме последних 10
- Кэш сборки Go
- Старые журналы systemd

## 🔍 Диагностика проблем

### Частые проблемы и решения

#### 1. Ошибка SSH подключения

```bash
# Проверьте подключение
./deploy/scripts/check-connection.sh --ip=95.142.40.244 --user=root

# Создайте новый SSH ключ
ssh-keygen -t rsa -b 4096 -f ~/.ssh/id_rsa_crypto -N ""

# Скопируйте на сервер
ssh-copy-id -i ~/.ssh/id_rsa_crypto.pub root@95.142.40.244

# Используйте новый ключ
./deploy/scripts/deploy.sh --ip=95.142.40.244 --user=root --key=~/.ssh/id_rsa_crypto
```

#### 2. База данных не подключена

```bash
# Проверьте PostgreSQL на сервере
ssh root@95.142.40.244 "systemctl status postgresql"

# Проверьте пользователя БД
ssh root@95.142.40.244 "sudo -u postgres psql -c '\du'"

# Пересоздайте пользователя
ssh root@95.142.40.244 << 'EOF'
sudo -u postgres psql -c "DROP DATABASE IF EXISTS crypto_screener_db;"
sudo -u postgres psql -c "DROP USER IF EXISTS crypto_screener;"
sudo -u postgres psql -c "CREATE USER crypto_screener WITH PASSWORD 'NewSecurePass123!';"
sudo -u postgres psql -c "CREATE DATABASE crypto_screener_db OWNER crypto_screener;"
EOF
```

#### 3. Приложение не запускается

```bash
# Проверьте логи
./deploy/scripts/service.sh logs 50 --ip=95.142.40.244

# Проверьте конфигурацию
./deploy/scripts/service.sh config-check --ip=95.142.40.244

# Проверьте права
ssh root@95.142.40.244 "ls -la /opt/crypto-screener-bot/bin/"
ssh root@95.142.40.244 "ls -la /opt/crypto-screener-bot/.env"
```

#### 4. Telegram бот не работает

1. Убедитесь, что `TELEGRAM_ENABLED=true`
2. Проверьте токен бота в BotFather
3. Убедитесь, что ваш Telegram ID правильный
4. Проверьте, что бот добавлен в чат/канал

## 📈 Обновление приложения

### Метод 1: Через скрипт обновления

```bash
# Обновление с перезапуском
./deploy/scripts/update.sh --ip=95.142.40.244 --user=root

# Горячее обновление (без остановки)
./deploy/scripts/update.sh --hot --ip=95.142.40.244
```

### Метод 2: Вручную

```bash
# Создайте резервную копию
./deploy/scripts/service.sh backup --ip=95.142.40.244

# Остановите службу
./deploy/scripts/service.sh stop --ip=95.142.40.244

# Скопируйте новые файлы
scp -r ./ root@95.142.40.244:/tmp/new-version/

# На сервере выполните
ssh root@95.142.40.244 << 'EOF'
cd /opt/crypto-screener-bot/src
cp -r /tmp/new-version/* .
go build -o ../bin/crypto-screener-bot ./application/cmd/bot/main.go
systemctl start crypto-screener
EOF

# Проверьте обновление
./deploy/scripts/service.sh status --ip=95.142.40.244
```

## 🗂️ Структура каталогов на сервере

```
/opt/crypto-screener-bot/
├── bin/                    # Исполняемые файлы
│   └── crypto-screener-bot
├── configs/               # Конфигурации
│   ├── dev/
│   ├── prod/
│   └── test/
├── src/                   # Исходный код
│   ├── application/
│   ├── internal/
│   ├── go.mod
│   └── go.sum
├── logs/                  # Симлинк на логи
├── data/                  # Данные приложения
└── .env -> configs/prod/.env # Текущая конфигурация

/var/log/crypto-screener-bot/  # Логи приложения
├── app.log
└── error.log

/opt/crypto-screener-bot_backups/  # Резервные копии
├── manual_backup_20240101_120000.tar.gz
└── manual_backup_20240102_130000.tar.gz
```

## 🔐 Безопасность

### Рекомендации по безопасности

1. **Смена паролей по умолчанию**
   ```bash
   # PostgreSQL
   ssh root@95.142.40.244 "sudo -u postgres psql -c \"ALTER USER crypto_screener WITH PASSWORD 'НовыйСложныйПароль123!';\""

   # Redis (в конфиге)
   ssh root@95.142.40.244 "sed -i 's/^# requirepass foobared/requirepass НовыйПарольRedis123!/' /etc/redis/redis.conf"
   ```

2. **Настройка брандмауэра**
   ```bash
   ssh root@95.142.40.244 << 'EOF'
   ufw default deny incoming
   ufw default allow outgoing
   ufw allow from YOUR_IP to any port 22
   ufw allow 22  # Или закомментируйте для отключения публичного доступа
   ufw --force enable
   EOF
   ```

3. **Регулярные обновления**
   ```bash
   ssh root@95.142.40.244 "apt update && apt upgrade -y"
   ```

4. **Мониторинг безопасности**
   ```bash
   # Установите fail2ban
   ssh root@95.142.40.244 "apt install -y fail2ban"

   # Установите logwatch
   ssh root@95.142.40.244 "apt install -y logwatch"
   ```

## 📊 Производительность

### Оптимизация настроек

1. **PostgreSQL настройки**:
   ```sql
   ALTER SYSTEM SET shared_buffers = '256MB';
   ALTER SYSTEM SET effective_cache_size = '768MB';
   ALTER SYSTEM SET maintenance_work_mem = '64MB';
   ALTER SYSTEM SET checkpoint_completion_target = 0.9;
   ALTER SYSTEM SET wal_buffers = '16MB';
   ALTER SYSTEM SET default_statistics_target = 100;
   ```

2. **Redis настройки**:
   ```bash
   # В /etc/redis/redis.conf
   maxmemory 512mb
   maxmemory-policy allkeys-lru
   ```

3. **Systemd лимиты**:
   ```bash
   # В /etc/systemd/system/crypto-screener.service
   LimitNOFILE=65536
   LimitNPROC=65536
   ```

## 🤝 Поддержка

### Получение помощи

1. **Сначала проверьте логи**:
   ```bash
   ./deploy/scripts/service.sh logs 100 --ip=95.142.40.244
   ```

2. **Проверьте конфигурацию**:
   ```bash
   ./deploy/scripts/service.sh config-check --ip=95.142.40.244
   ```

3. **Проверьте здоровье системы**:
   ```bash
   ./deploy/scripts/service.sh health --ip=95.142.40.244
   ```

4. **Используйте мониторинг**:
   ```bash
   ./deploy/scripts/service.sh monitor --ip=95.142.40.244
   ```

### Отладка

```bash
# Подробный вывод скрипта
bash -x ./deploy/scripts/deploy.sh --ip=95.142.40.244

# Проверка SSH подключения
ssh -vvv -i ~/.ssh/id_rsa root@95.142.40.244

# Проверка сети
traceroute 95.142.40.244
mtr 95.142.40.244

# Проверка портов
nc -z -w 2 95.142.40.244 22
nc -z -w 2 95.142.40.244 5432
nc -z -w 2 95.142.40.244 6379
```

## 📝 Чеклист развертывания

### Перед началом
- [ ] IP сервера доступен
- [ ] SSH ключ сгенерирован и скопирован
- [ ] Конфиг `configs/prod/.env` проверен
- [ ] Репозиторий обновлен до последней версии

### После развертывания
- [ ] Сервис запущен (`systemctl status crypto-screener`)
- [ ] Логи без ошибок (`journalctl -u crypto-screener`)
- [ ] База данных доступна
- [ ] Конфигурация настроена правильно
- [ ] Telegram бот работает (если включен)

### Регулярное обслуживание
- [ ] Проверять логи на ошибки
- [ ] Мониторить использование ресурсов
- [ ] Создавать резервные копии
- [ ] Обновлять приложение
- [ ] Обновлять систему безопасности

---

**Примечание**: Все скрипты разработаны для Ubuntu 22.04. Для других версий могут потребоваться изменения. Всегда тестируйте изменения на тестовом сервере перед применением на продакшене.

## 📄 Лицензия

[Укажите вашу лицензию]

---

*Последнее обновление: $(date +%Y-%m-%d)*