-- Список монет для отслеживания (вотчлист пользователя).
-- NULL = отслеживать все монеты (поведение по умолчанию, обратная совместимость).
-- Непустой массив = отслеживать только эти символы.
ALTER TABLE users
    ADD COLUMN IF NOT EXISTS watchlist_symbols TEXT[] DEFAULT NULL;
